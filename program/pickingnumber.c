#include<stdio.h>
int main()
{
	int n,i,key;
	scanf("%d",&n);
	int a[n],b[n];
	for(i=0;i<n;i++)
	scanf("%d",&a[i]);
	key=a[0];
	for(i=1;i<n;i++)
	{
		if(abs(key-a[i])<=1)
		{
			
		}
		
		
	}
}
